using XLua;
﻿using UnityEngine;
using System.Collections;

[Hotfix]
public class EffectOnWidget : MonoBehaviour {

    //public GameObject m_effectObj;


    public UIWidget m_inWidget; 

	// Use this for initialization
	void Start () {
	
	}
	
	// Update is called once per frame
    //void Update () {
    //    Bounds bound = m_inWidget.CalculateBounds();
    //    bool isIn = bound.Contains(transform.position);
 
         
    //    if (isIn == false)
    //    {
    //        return; 
    //    }
    //    //if  (transform.position.x < m_inWidget.
    //}
}

