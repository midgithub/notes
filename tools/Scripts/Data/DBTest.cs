using XLua;
﻿using UnityEngine;
using System.Collections;

[Hotfix]
public class DBTest : MonoBehaviour {

	// Use this for initialization
	void Start () 
	{
        //for (int i = 0; i < Res_FileDB.g_resFileDB.SkillDescCount; ++i)
        //{
        //    gamedb.SkillDesc res = Res_FileDB.g_resFileDB.GetSkillDesc(i);
        //    int id 	= res.iID;
        //    byte[] actionName = System.Text.Encoding.Convert (System.Text.Encoding.UTF8, System.Text.Encoding.Default, res.szAttackAction);
        //    string sActionName	= System.Text.Encoding.Default.GetString(actionName).Replace("\0",null);
        //    float hitscale = res.stHitMove.fScale;
        //    float hittime = res.stHitMove.fTime;
        //    int canbreak = res.chCanBreak;
			
        //    LogMgr.UnityLog ("res id:" + id.ToString() + " action:" + sActionName.ToString() + " hitscale:" + hitscale.ToString() + " hittime:" + hittime.ToString() + " canbreak:" + canbreak.ToString());
        //}
	}
	
	//// Update is called once per frame
	//void Update () {
	//
	//}
}

