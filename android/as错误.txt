gradle dependency cache may be corrupt:
gradle-wrapper.properties里修改了gradle的版本，与之前没有报错的gradle版本一致
  http\://services.gradle.org/distributions/gradle-2.10-all.zip  
(gradle-3.3-all.zip)不能下载改成2.10

修改Manifest.xml
<activity
    android:name=".MainActivity"
    android:theme="@style/Theme.AppCompat">  //没有这个会闪退

    <intent-filter>
        <action android:name="android.intent.action.MAIN"/>

        <category android:name="android.intent.category.LAUNCHER"/>
    </intent-filter>
</activity>

导入工程为module为 liblary
settings.gradle 回加include ':libname'

1.修改 build.gradle 为 apply plugin: 'com.android.library'，去掉applicationId "com.google.zxing.client.android"，改dependencies
2.修改AndroidManifest：
注释，否则安装APP时候，手机上会出现两个图标
<!--<intent-filter>-->
	<!--<action android:name="android.intent.action.MAIN"/>-->
	<!--<category android:name="android.intent.category.LAUNCHER"/>-->
<!--</intent-filter>-->
3.Suggestion: add 'tools:replace="android:icon"' to element
因为 Library项目中也定义了与主项目相同的属性
解决 根标签下引入xmlns:tools="http://schemas.android.com/tools"
     application里的标签里加入tools:replace="android:icon"  多个属性用,隔开 =”android:icon, android:theme”

4.常量表达式的错误：这个错误是switch case的问题，提示换成if else

5.app build.gradle添加引用 compile project(path: ':libname')
6.编译好的aar文件在build\outputs\aar目录下

7.app 引用 arr:
build.gradle添加引用:
repositories {
    flatDir {
        dirs 'libs'
    }
}
compile(name:'libname', ext:'aar')

Error:Execution failed for task ':app:processDebugManifest':
导入的库在build.gradle中的minSdkVersion与你的应用的minSdkVersion不匹配导致的,改成一样的

编译完成后，在app/build/intermediates/exploded-aar/可以看见我们引用的aar包内容
(配置项目目录下的 gradle.properties,添加 android.enableBuildCache=false,新版默认生成一部分缓存文件，不会有该文件夹)