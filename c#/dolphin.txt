//启动普通和灰度用户更新，要至少一个相对应的版本， 灰度用户还必须在白名单中，要不会报网路错误
//apk版本 支持差异更新（通过文件二进制对比生成差异文件），资源版本不支持（Zip格式），但可以使用首包解压
//首包解压可选，非必须，目前业务基本都会使用。如果有首包解压，每次资源版本发布就是全量的资源打包一个zip发布；
  如果没有首包解压，每次资源版本发布就是将全量的差异资源打包一个zip发布。
//每个程序版本至少一个对应的资源版本，要不资源更新报网路错误
//没有 first_source，首包解压检查更新报系统错误
//资源更新只做文件替换和增加，不做文件删除(如何删除废弃资源，资源版本可以自行处理)